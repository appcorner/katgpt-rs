# Issue 875: PFD horizon weighting + target-anchored renoise probe (Research 582 modelless arm)

**Status:** OPEN — T1 CLOSED + T2 CLOSED (GOAT PASS, Bench 877, opt-in combined-gate) + T4 CLOSED (GOAT PASS, Bench 879, opt-in `renoise_ce_surprise`); T3 gated on riir-train 569 C9 (the only open task); T5 CLOSED 2026-09-23 (consolidated verdict: all measured arms stay OPT-IN — no promotion, no retirement; the no-default-consumer rule)

**Source:** [Research 582](../.research/582_Probability_Flow_Distillation_Wasserstein_Gradient_Flow.md) (arXiv:2605.09071 Probability-Flow Distillation). Sibling filings: riir-train `.issues/569` (training arm), riir-ai `.issues/996` (crowd fusion).

**Problem.** Three paper-stated closed forms with in-stack consumers ship nowhere in the workspace:

1. **The (T−t) horizon weighting law** — averaging a uniformly-sampled-t partial integral gives effective weight (T−s) (Fubini swap on the triangular domain): low-noise observations get max weight, decaying linearly to **exactly zero at t=T**. `twist_step_into` is span-normalized β/KL-budget; `renoise_ce` averages k_draws flat; `dllm_solver` switching is entropy- (state-) driven — nothing weights by remaining horizon.
2. **The w(t) closed-form schedule** `w(t) = ½(T−t)g(t)²c(t,0)²` with `c(t,0)=exp(−∫a)` — offline-computable over a discrete t-grid → a committed O(1) lookup table (the `katgpt-attn/static_cal.rs` Sinkhorn→table precedent, except this table is exact closed form, no iteration).
3. **Time-annealed t-sampling range** — iteration-count-indexed anneal of the sampled noise range toward low noise (orthogonal to the shipped state-indexed entropy switching), plus the zero-terminal-weight truncation corollary (skipping t near T is first-order lossless because w(T)=0).

Plus one probe mode:

4. **Target-anchored round-trip** — `renoise_ce` ships perturb→re-resolve-through-the-SAME-operator→drift; PFD's variant resolves against a TARGET prior (frozen scorer), turning the drift score from self-consistency into distributional surprise against the target. Consumer: consolidation surprise ranking (which shards enter the Raven/δ-Mem sleep cycle first) — the `[f32; 8]` score convention already fits `NeuronShard.hla_moments`.

## Tasks

- [x] **T1** `horizon_weights` utility in katgpt-core (pure fn + optional committed table, BLAKE3 `commit`/`verify` per the StaticCalTable pattern): `w(t) = ½(T−t)g(t)²c(t,0)²` over a discrete grid; normalized (T−s)/T variant for generic horizon weighting. G1: table vs closed-form recompute bit-match; G2 lookup vs per-call exp/pow (ns/op); G4 fixed `[f32; N]`, zero-alloc. **Name-collision note:** `horizon_decay` already exists in `katgpt-core` `hint_regret/memory.rs` — that is `r̂·σ(−λ·Δt)` staleness fading (mechanism-distinct from remaining-horizon accumulation weighting); name the new utility to avoid the collision (e.g. `remaining_horizon_weights`).
- [x] **T2** Wire T1 as an opt-in weighting in ONE consumer: `renoise_ce` k_draws averaged under (T−t) weights (draw t non-uniformly per the law) — vs flat averaging. Quality gate: selectivity precision@k against a planted-drift oracle on a synthetic mixture; latency unchanged class.
- [ ] **T3** Time-anneal struct for `dllm_solver` sampling ranges (`[0.02T, 0.98T] → [0.02T, 0.70T]` late-iteration shape, iteration-indexed) + zero-terminal-weight truncation predicate. Gate: quality-at-fixed-budget vs flat range on the toy harness built by riir-train 569 C9 (shared fixture; do not duplicate).
- [x] **T4** Target-anchored `RenoiseCeProbe` probe mode (re_resolve against a caller-supplied target scorer) + a consolidation-ordering sketch consumer (score rides existing passes; ordering-only, no behavior change). Gate: precision@k vs oracle novelty (drift injected at known shards).
- [x] **T5** GOAT verdict per task (G1 correctness, G2 perf, G3 no-regression, G4 alloc) recorded here; promote-to-default only on modelless gain; demote/retire losers.


**T1 GOAT verdict (2026-09-22, commit 02d5813d):** PASS - G1 table/pure-fn bit-match (to_bits) in-module + e2e root gate, independent f64 oracle rel < 1e-5; G2 (release, shared ab_timing harness) table lookup 2.13 ns/op vs the STRONG per-call baseline (caller-held integral, one exp) 9.68 ns/op - median b/a 0.2222 (4.5x faster), 41/41 interleaved rounds; G3 opt-in + default-off (no default-path surface); G4 fixed [f32; 64], 0 allocs on build + 1024 lookups (TrackingAllocator). Stays opt-in per the no-default-consumer rule - promotion rides T2.

**Non-goals:** KL `MeasureReward` row (documented module non-goal — density estimation; `MmdReward` is the shipped measure-level analog); negative-CFG helper (no consumer in katgpt-core — routed to riir-train 569 C4); any W2-geometry claim (the exactness theorem is continuous-space, linear-drift scoped — we adopt the schedules, not the theorem).

**T2 GOAT verdict (2026-09-22, Bench 877):** PASS - G2-QUALITY planted-drift selectivity oracle (32 stable + 32 planted, deterministic seed): precision@32 = **1.000 horizon vs 0.906 incumbent-fixed vs 0.938 uniform-range control** (the control isolates the LAW from the RANGE; sim sweep 4 noise-mult x 4 bands x 60 seeds: no-loss 60/60 at the chosen band, ordering horizon > uniform > fixed held in every config). G2-LATENCY (release, shared ab_timing): median horizon/incumbent **0.9916** (-0.8%) at equal k=8 budget - unchanged class. G1 determinism bit-pinned + sampler endpoints/monotone/CDF-buckets/f64-oracle (11 new in-module tests). G3 default path untouched: default lib suite 2063/0, bench_406 5/5, no-default-features check clean, clippy -D warnings clean at default / horizon_weights / new test target. G4 zero alloc on the score path (fixed-array State, TrackingAllocator). Shipped as `renoise_ce_score_horizon` + `RenoiseCeHorizon` (combined-gate renoise_ce+horizon_weights; L = perturbation_level as the horizon; weights ride the tilted sampling distribution - importance sampling, never double-applied) + `remaining_horizon_t_sample` added to the T1 substrate (exact inverse CDF, one sqrt, zero alloc). Stays OPT-IN; tau does NOT transfer between modes; defect classes visible ONLY at high t favor the fixed arm (regime boundary named in-module + Bench 877).

**T4 GOAT verdict (2026-09-23, Bench 879):** PASS - shipped as `renoise_ce_surprise` (opt-in `renoise_ce_surprise = ["renoise_ce"]`, katgpt-core + root passthrough; the three score fns now share one private `renoise_k_draws` loop — incumbent RNG stream + float op order preserved, default lib 2063/0 + bench_406 5/5 post-refactor). G2-QUALITY flow-relative-novelty oracle (shell world: 32 stable + 32 planted, all candidates EQUIDISTANT from the prior anchor — plain distance blind by construction): precision@32 = **1.000 surprise vs 0.000 incumbent (self-consistency ANTI-ranks foreign-basin novelty — the measured motivating gap) vs 0.500 plain-distance** (python mirror 200 seeds: 1.000±0.002 / 0.000 / 0.428, margins ≥ 2 candidates 200/200). G2-LATENCY median surprise/incumbent **0.9994** (-0.1%, release, shared ab_timing, 41/41 rounds) — unchanged class (identical loop; only the drift reference changed). G3 consumer sketch: consolidation surprise ordering (Raven/δ-Mem sleep-cycle admission = the surprise-ranked head) ≥0.90 planted at HALF budget (k=4) + deferral tail ≥0.90 stable; ordering-only, no behavior change. G4 zero-alloc (fixed-array State, TrackingAllocator, in-module). G3 no-regression: default 2063/0, `--no-default-features` ×2, wasm32 ×2, clippy -D warnings ×3 postures. REGIME BOUNDARY (Bench 879): where pointwise distance already sees displacement, plain distance is a strong ranker (0.87-0.95+ in the v5/v6 sim sweeps) and the mode adds little — the win is FLOW-RELATIVE novelty (basin membership invisible pointwise); single-reading obs noise caps ANY arm at the Φ(−s/2) information limit (multi-reading window states are the consumer-side remedy). tau does NOT transfer (anchor changed). Stays OPT-IN per the T5 promotion rule.

**T5 GOAT verdict (2026-09-23, consolidated; Claude verdict ping-pong round-2 adopted):** NO PROMOTION, NO RETIREMENT — all three measured arms stay OPT-IN; the issue remains OPEN on T3 alone.
(1) The promotion blocker is the house no-default-consumer rule, stated precisely: `renoise_ce` is default-on and every arm is an ADDITIVE sibling fn behind its cfg gate (Bench 877 G3 — incumbent body unchanged; Bench 879 — the shared `renoise_k_draws` refactor preserved the incumbent's RNG stream + float op order, default 2063/0 + bench_406 5/5), so a default flip would compile additional API surface and change ZERO existing callers — but no default-path CALL SITE of `renoise_ce_score_horizon` / `renoise_ce_surprise` exists, making promotion pure default-surface growth with no default-path behavior gain. T1 rides T2 per its own in-file rule.
(2) The regime boundaries (T2: high-t-only defect classes favor the fixed arm; T4: pointwise-visible displacement → plain distance 0.87-0.95+) are CALLER-SELECTION guidance, not promotion blockers and not retirement candidates — the controls that lost (uniform-range, plain-distance) were never shipped, and the incumbent `renoise_ce_score` is untouched and still correct for its consumers.
(3) T3 stays gated, blocked-not-measured: riir-train `.issues/569` C9 unchecked as of 2026-09-23 and its parent plan explicitly staged behind it ("toy-harness gate FIRST — no 4090 spend before C9 passes").
(4) RE-GATE TRIGGER: promotion re-opens when a default-path caller appears (T4's natural consumer = riir-neuron-db consolidation admission — **measured DEAD on that operator 2026-09-23, see the addendum below**; T2's = any `renoise_ce` caller on a default path); at re-gate both benches already declare these NOT UQ primitives (ranking signal only), so the conformal-naive floor rule (Research 322 / Plan 340) is out of scope. Citation repairs riding the T5 commit: katgpt-core Cargo.toml's `horizon_weights` comment re-addressed Bench 878→877 (878 is Plan 607 T3's state-option-head GOAT); Bench 879's two `renoince_ce_surprise` misspellings fixed; catalog rows 123/124 added (`.docs/09_feature_catalog/opt_in_features.md`).

**T5 addendum (2026-09-23, measured negative — the named ndb consumer is DEAD on its operator):** the T4 re-gate trigger's natural consumer (riir-neuron-db consolidation surprise ordering, filed in the 879 G3 consumer-sketch arm) was probed against that crate's actual operator before any wiring, per the negative-result cross-run rule (Issue 825 class). Result: **mechanism cannot transfer — recorded-not-wired** (ndb `HISTORY.md` 2026-09-23 entry carries the mirror record).

Mechanism: ndb's wake→sleep operator is LINEAR (`average_embeddings` + `blend_style_delta`'s plain lerp). The probe perturbs one candidate event and re-resolves the SET; under single-member perturbation the set average is `F(x_i + ε) = (Σ + ε)/n` — independent of WHICH member was perturbed (`rest_i = Σ − x_i`, the total is fixed). The re-resolved state, hence the target-anchored drift, is EVENT-INDEPENDENT up to RNG jitter: the surprise ranking is pure noise. Measured with the shipped primitive (scratch probe mirroring ndb's `proactive_freeze_drift` ±uniform perturbation, `RenoiseCeConfig::DEFAULT`, D=64, 33 events = 32 stable near a single-mode prior + 1 planted-novel, 5 probe seeds): every event scored **0.000082** (uniform to 6 dp); the 5 seeds produced **5 different rankings**; the planted-novel event **buried at #26/33** under the probe while a plain leave-one-out distance ranked it **#1/33**.

Read with Bench 879's own regime boundary: where displacement is pointwise-visible, plain distance is the correct ranker — and ndb's pipeline already ships it everywhere (info density, MAG transfer, cosine). Any future surprise-ranked admission ordering there should use the closed-form leave-one-out distance (O(n·d), zero RNG), never this probe. **The re-gate trigger survives but narrows:** promotion re-opens only on a consumer whose operator is event-sensitive / basin-structured (nonlinear re-resolve — e.g. a greedy/selection operator like the AM/OMP compactor side, unsized and unscheduled). The shell-world bench remains the proof the mechanism is real where basins exist; this addendum scopes WHERE it applies.
